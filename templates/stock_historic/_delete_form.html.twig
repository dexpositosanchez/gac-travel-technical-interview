<form method="post" action="{{ path('app_stock_historic_delete', {'id': stock_historic.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stock_historic.id) }}">
    <button  data-toggle="tooltip" data-placement="top" title="" data-original-title="Eliminar" class="badge bg-danger"><i class="las la-trash"></i></button>
</form>
