{% extends 'base.html.twig' %}

{% block title %}StockHistoric{% endblock %}

{% block body %}
    <h1>StockHistoric</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stock_historic.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ stock_historic.createdAt ? stock_historic.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Stock</th>
                <td>{{ stock_historic.stock }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_stock_historic_index') }}">back to list</a>

    <a href="{{ path('app_stock_historic_edit', {'id': stock_historic.id}) }}">edit</a>

    {{ include('stock_historic/_delete_form.html.twig') }}
{% endblock %}
