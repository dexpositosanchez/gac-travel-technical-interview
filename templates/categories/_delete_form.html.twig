<form method="post" action="{{ path('app_categories_delete', {'id': category.id}) }}" onsubmit="return confirm('¿Estás seguro de eliminar este registro?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}">
    <button class="btn btn-primary mr-2">Eliminar</button>
</form>
