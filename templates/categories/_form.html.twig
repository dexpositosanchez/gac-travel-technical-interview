{{ form_start(form) }}
    <div class="row">
        <div class="col-md-12">                  
            <div class="form-group">
                {{ form_label(form.name,'Nombre *') }}
                {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}

                <div style='display:none;'>
                    {{ form_widget(form.createdAt) }}
                </div>
            </div>
        </div>                                                       
        <div class="col-md-12 mt-5 text-center">
            <a class="btn btn-primary mr-2" href="{{ path('app_categories_index') }}">Volver</a>
            <button type="submit" class="btn btn-primary mr-2">{{ button_label|default('Guardar') }}</button>
        </div>
    </div>
{{ form_end(form) }}