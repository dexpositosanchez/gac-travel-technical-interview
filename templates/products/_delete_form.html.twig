<form method="post" action="{{ path('app_products_delete', {'id': product.id}) }}" onsubmit="return confirm('¿Estás seguro de eliminar este registro?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id) }}">
    <button class="btn btn-primary mr-2">Eliminar</button>
</form>